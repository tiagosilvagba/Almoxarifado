"use strict";
const CSV_CACHE_NAME="almoxarifado-csv-v48";
const APP_CACHE_NAME="almoxarifado-app-v48";
const CSV_PATTERN=/\.csv(?:$|\?)/i;
const SCRIPT_PATTERN=/\/(?:script|responsive-layout|github-commit-queue|github-photo-upload-v2|page-snapshot-pdf|saldo-update-time|area-filter-active|layout-fix|comparativo-[a-z0-9-]+)\.js$/i;
const STYLE_PATTERN=/\/(?:style|responsive-project)\.css$/i;
self.addEventListener("install",()=>self.skipWaiting());
self.addEventListener("activate",event=>{event.waitUntil((async()=>{const names=await caches.keys();await Promise.all(names.filter(name=>name.startsWith("almoxarifado-")&&![CSV_CACHE_NAME,APP_CACHE_NAME].includes(name)).map(name=>caches.delete(name)));await self.clients.claim();const clients=await self.clients.matchAll({type:"window",includeUncontrolled:true});for(const client of clients)client.postMessage({type:"almoxarifado-app-cache-refreshed"})})())});
self.addEventListener("fetch",event=>{const request=event.request;if(request.method!=="GET")return;const url=new URL(request.url);if(url.origin!==self.location.origin)return;if(CSV_PATTERN.test(url.pathname+url.search)){event.respondWith(csvCacheFirst(request,event));return}if(SCRIPT_PATTERN.test(url.pathname)||STYLE_PATTERN.test(url.pathname)||request.mode==="navigate")event.respondWith(appNetworkFirst(request))});
async function csvCacheFirst(request,event){const cache=await caches.open(CSV_CACHE_NAME),cached=await cache.match(request.url);if(!cached){const response=await fetchFresh(request);if(response.ok)await cache.put(request.url,response.clone());return response}event.waitUntil(revalidateCsv(request,cached,cache));return cached}
async function appNetworkFirst(request){const cache=await caches.open(APP_CACHE_NAME);try{const response=await fetch(request,{cache:"no-store"});if(response.ok)await cache.put(request.url,response.clone());return response}catch(error){const cached=await cache.match(request.url);if(cached)return cached;throw error}}
async function revalidateCsv(request,cached,cache){try{const headers=new Headers(request.headers),etag=cached.headers.get("etag"),lastModified=cached.headers.get("last-modified");if(etag)headers.set("If-None-Match",etag);if(lastModified)headers.set("If-Modified-Since",lastModified);const response=await fetch(request.url,{method:"GET",headers,cache:"no-store",credentials:request.credentials,mode:request.mode,redirect:request.redirect});if(response.status===304||!response.ok)return;if(await sameResponse(cached,response))return;await cache.put(request.url,response.clone());await notifyClients(request.url)}catch{}}
async function fetchFresh(request){return fetch(request.url,{method:"GET",headers:request.headers,cache:"no-store",credentials:request.credentials,mode:request.mode,redirect:request.redirect})}
async function sameResponse(cached,fresh){const ce=cached.headers.get("etag"),fe=fresh.headers.get("etag");if(ce&&fe)return ce===fe;const cl=cached.headers.get("content-length"),fl=fresh.headers.get("content-length"),cm=cached.headers.get("last-modified"),fm=fresh.headers.get("last-modified");if(cl&&fl&&cm&&fm)return cl===fl&&cm===fm;return false}
async function notifyClients(url){const clients=await self.clients.matchAll({type:"window",includeUncontrolled:true});for(const client of clients)client.postMessage({type:"almoxarifado-csv-updated",url})}
